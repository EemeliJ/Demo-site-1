export default function Nested() {
    return <h1>This is the nested page under nest</h1>
}

/* not ideal for example an E-commerce site with thousands of products, thats where dynamic routing comes in,
see products/[id]*/
